class TaskData{
  final String task;

  const TaskData({this.task});
}

const kTask = const<TaskData>[
  const TaskData(
    task: 'Throw my old PC',
  ),
  const TaskData(
    task: 'Get a new PC',
  )
];

